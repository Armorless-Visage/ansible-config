---
# (c) Armorless-Visage 2018 ISC
# prep system for scap role


# comments interfere with the ansible remediation roles generated by oscap
- name: remove comments from /etc/ssh/sshd_config
  replace:
    dest: /etc/ssh/sshd_config
    regexp: "^#*$"
    validate: sshd -t -f %s
  tags:
    - prep
    - nocomments
    - ssh
    
